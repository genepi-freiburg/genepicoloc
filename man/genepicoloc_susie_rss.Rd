% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genepicoloc_susie.R
\name{genepicoloc_susie_rss}
\alias{genepicoloc_susie_rss}
\title{Run SuSiE RSS with summary statistics}
\usage{
genepicoloc_susie_rss(
  sumstats,
  LD_matrix,
  Name_name = "Name",
  BETA_name = "BETA",
  SE_name = "SE",
  N_name = "N",
  N,
  coverage = 0.95,
  ...
)
}
\arguments{
\item{sumstats}{Data.frame. Summary statistics with columns specified by
the name parameters.}

\item{LD_matrix}{Numeric matrix. LD correlation matrix with variants in the
same order as sumstats. Row and column names must match sumstats Name column.}

\item{Name_name}{Character string. Column name for variant identifiers
(default: "Name").}

\item{BETA_name}{Character string. Column name for effect sizes
(default: "BETA").}

\item{SE_name}{Character string. Column name for standard errors
(default: "SE").}

\item{N_name}{Character string. Column name for sample sizes
(default: "N").}

\item{N}{Numeric. Sample size override. If not provided, uses median from
the N column.}

\item{coverage}{Numeric. Coverage for credible sets (default: 0.95).}

\item{...}{Additional arguments passed to susie_rss().}
}
\value{
A susie object containing fine-mapping results.
}
\description{
Performs fine-mapping using SuSiE RSS (Regression with Summary Statistics)
which requires only summary statistics and an LD matrix.
}
\details{
Key parameters that can be passed via ...:
\itemize{
\item L: Maximum number of causal variants (default: 10)
\item max_iter: Maximum iterations (default: 100)
\item estimate_residual_variance: Whether to estimate residual variance
\item refine: Whether to refine the model
}

The function requires that variant order in sumstats matches the order
in the LD matrix exactly.
}
\examples{
\dontrun{
# Fine-mapping with summary statistics
susie_results <- genepicoloc_susie_rss(
  sumstats = my_sumstats,
  LD_matrix = my_ld_matrix,
  N = 10000,
  coverage = 0.95,
  L = 10
)
}

}
